let fruits = ["apple", "orange", "banana"];
// fruits.push("coconut"); // adds to the end
// fruits.pop(); // pops last element
// fruits.unshift("mango"); // adds to the beginning
// fruits.shift(); // pops first item
// console.log(fruits.length); // length of the array
// console.log(fruits.indexOf("orange")); // Finds an element id in array. If it's not in the array you will get -1.

// for (let i = 0; i <fruits.length; i++) console.log(fruits[i]);
// for (let fruit of fruits) console.log(fruit);

// fruits.sort(); //sort in alphabetical order
// fruits.sort().reverse(); //sort in reverse alphabetical order

console.log(fruits);